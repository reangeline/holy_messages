// GERADO — não editar à mão.
// Origem: ~/Documents/Missale-pesquisa/entregas/exame-revisado, importado por
// scripts/import_mood_relief.py. Enquanto os três catálogos não existem, as
// coleções vazias mantêm o catálogo já revisado de português como fallback.
//

import Foundation

extension MockMood {
    static let ptReviewedReliefByState: [String: [ReliefContent]] = [:]
    static let enReviewedReliefByState: [String: [ReliefContent]] = [:]
    static let esReviewedReliefByState: [String: [ReliefContent]] = [:]
}
